# Remote AI Agent Configuration

# Anthropic API Key (required)
# The agent uses this to interpret natural language instructions
ANTHROPIC_API_KEY=sk-ant-api03-...

# Agent API Key (optional but recommended)
# Clients must provide this key to execute tasks
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
REMOTE_AGENT_API_KEY=your-secure-api-key-here

# Agent Port (default: 8001)
REMOTE_AGENT_PORT=8001
