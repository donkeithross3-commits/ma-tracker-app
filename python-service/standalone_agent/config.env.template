# IB Data Agent Configuration
# ============================
# Copy this file to config.env and fill in your API key.

# Your API key from the MA Tracker web app (REQUIRED)
# Get this from: Settings > IB Connection > Download Agent
IB_PROVIDER_KEY=your-api-key-here

# IB TWS/Gateway connection settings (optional - defaults shown)
# IB_HOST=127.0.0.1
# Port (optional): TWS default is 7497=paper, 7496=live. Agent tries both if one fails.
# IB_PORT=7497
# IB_MODE=paper
# Client ID (optional): Use a unique value per agent when running multiple agents
# against the same TWS. Default is 0 (receives TWS-owned orders).
# IB_CLIENT_ID=0

# WebSocket relay URL (optional - uses default if not set)
# RELAY_URL=wss://dr3-dashboard.com/ws/data-provider

# Exclude specific IB accounts from positions/orders (comma-separated)
# Useful when TWS shows sub-accounts you don't want in the dashboard (e.g. managed accounts)
# IB_EXCLUDE_ACCOUNTS=U22621569

# Polygon.io API key (required for BMC strategy real-time market data)
# POLYGON_API_KEY=your-polygon-key-here
