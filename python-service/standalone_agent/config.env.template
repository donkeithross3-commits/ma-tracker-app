# IB Data Agent Configuration
# ============================
# Copy this file to config.env and fill in your API key.

# Your API key from the MA Tracker web app (REQUIRED)
# Get this from: Settings > IB Connection > Download Agent
IB_PROVIDER_KEY=your-api-key-here

# IB TWS/Gateway connection settings (optional - defaults shown)
# IB_HOST=127.0.0.1
# Either set IB_PORT explicitly, or use IB_MODE to pick paper vs live:
#   IB_PORT=7497   (paper) or IB_PORT=7496 (live)
#   IB_MODE=paper  -> 7497   IB_MODE=live  -> 7496
# IB_PORT=7497
# IB_MODE=paper

# WebSocket relay URL (optional - uses default if not set)
# RELAY_URL=wss://dr3-dashboard.com/ws/data-provider
